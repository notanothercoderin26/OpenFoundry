// @generated by protoc-gen-connect-es v1.6.1
// @generated from file marketplace/v1/product.proto (package open_foundry.marketplace.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { CreateProductRequest, GetProductRequest, Installation, InstallProductRequest, ListInstallationsRequest, ListInstallationsResponse, ListProductsRequest, ListProductsResponse, Product, ProductVersion, PublishVersionRequest, UninstallRequest } from "./product_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * MarketplaceProductService is the read/write surface for the Foundry
 * "Marketplace products" feature: a Product is a reusable bundle of
 * Ontology types, Action types, Pipelines and Workshop/Slate Apps that
 * can be packaged once and installed many times into different target
 * workspaces.
 *
 * The Go service (federation-product-exchange-service) exposes the same
 * surface over JSON+REST on /api/v1/marketplace/products. This proto is
 * the canonical contract; the JSON shapes mirror the message fields
 * 1:1 (snake_case) so SDKs generated from this file stay in sync with
 * the handlers.
 *
 * @generated from service open_foundry.marketplace.v1.MarketplaceProductService
 */
export declare const MarketplaceProductService: {
  readonly typeName: "open_foundry.marketplace.v1.MarketplaceProductService",
  readonly methods: {
    /**
     * Creates a Product in DRAFT status. Resources can still be added or
     * removed up until a version is published. Returns the new Product.
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.CreateProduct
     */
    readonly createProduct: {
      readonly name: "CreateProduct",
      readonly I: typeof CreateProductRequest,
      readonly O: typeof Product,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Lists products optionally filtered by status. Page/cursor pagination
     * mirrors the listings endpoint shape (limit + offset).
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.ListProducts
     */
    readonly listProducts: {
      readonly name: "ListProducts",
      readonly I: typeof ListProductsRequest,
      readonly O: typeof ListProductsResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * GetProduct returns a single product by rid.
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.GetProduct
     */
    readonly getProduct: {
      readonly name: "GetProduct",
      readonly I: typeof GetProductRequest,
      readonly O: typeof Product,
      readonly kind: MethodKind.Unary,
    },
    /**
     * PublishVersion takes a snapshot of every resource referenced by
     * the product, packages it into a tar.gz bundle whose root contains
     * a manifest.json (signed with HMAC-SHA256 using MARKETPLACE_SIGN_KEY)
     * plus per-resource folders, uploads the bundle to object storage,
     * and records a ProductVersion row.
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.PublishVersion
     */
    readonly publishVersion: {
      readonly name: "PublishVersion",
      readonly I: typeof PublishVersionRequest,
      readonly O: typeof ProductVersion,
      readonly kind: MethodKind.Unary,
    },
    /**
     * InstallProduct downloads the published bundle, verifies its
     * signature, then re-creates each resource in the target workspace
     * by calling the owning service. The returned Installation records
     * the mapping between the source ref (snapshotted in the bundle)
     * and the new rid in the target workspace.
     *
     * The (product_rid, version, target_workspace_rid) triple is unique:
     * installing the same bundle twice into the same workspace is a
     * no-op and returns the prior Installation unchanged.
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.InstallProduct
     */
    readonly installProduct: {
      readonly name: "InstallProduct",
      readonly I: typeof InstallProductRequest,
      readonly O: typeof Installation,
      readonly kind: MethodKind.Unary,
    },
    /**
     * ListInstallations returns installations filtered optionally by
     * target_workspace_rid.
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.ListInstallations
     */
    readonly listInstallations: {
      readonly name: "ListInstallations",
      readonly I: typeof ListInstallationsRequest,
      readonly O: typeof ListInstallationsResponse,
      readonly kind: MethodKind.Unary,
    },
    /**
     * Uninstall flips an INSTALLED installation back to UNINSTALLED.
     * Resource cleanup on the target services is best-effort and is the
     * workspace owner's responsibility (v1 does not cascade deletes).
     *
     * @generated from rpc open_foundry.marketplace.v1.MarketplaceProductService.Uninstall
     */
    readonly uninstall: {
      readonly name: "Uninstall",
      readonly I: typeof UninstallRequest,
      readonly O: typeof Installation,
      readonly kind: MethodKind.Unary,
    },
  }
};

